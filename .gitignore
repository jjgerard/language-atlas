node_modules/
# Geometry sources: ~65 MB, downloaded on first build, reproducible from
# build-geometry.js
sources/
# Optional cold-start fallback written by `npm run snapshot`. Not committed:
# shipping a stale copy of policy data risks serving it without anyone noticing.
data/snapshot.json
